/**
 * register the url
 */
function register() {
}

/**
 * validate the url
 * @param swUrl {string} - url to validate
 */
function registerValidSW(swUrl) {
}

/**
 * determine whether service worker is valid
 * @param swUrl {string} - url to validate
 */
function checkValidServiceWorker(swUrl) {
}

/**
 * close the service worker
 */
function unregister() {
}
